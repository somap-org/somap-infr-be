# NOTE: update this with your service name
service:
  name: somap-infr-be
  alias: somap
provider:
  name: aws
  runtime: nodejs10.x
  stage: dev
  region: us-east-1
  accountId: 482385118572
resources:
  # Cognito
  - ${file(resources/cognito/cognito-user-pool.yml)}
  - ${file(resources/cognito/cognito-identity-pool.yml)}
